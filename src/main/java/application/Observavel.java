package application; 

// todo método dentro de uma interface entende-se como público

public interface Observavel {
    String descricao = "Verifica os atributos de uma classe";
    
    String getClassStatus();
}